services:
  - type: web
    name: restoran-menyu-tizimi
    env: node
    region: oregon
    plan: free
    branch: main
    buildCommand: |
      npm install
      npm run build
      echo "📦 Verifying build output..."
      ls -laR server/dist/
      echo "🔍 Checking for index.html..."
      find server/dist -name "index.html" -type f
      if [ ! -f server/dist/index.html ]; then echo "❌ ERROR: index.html not found after build!"; exit 1; fi
      echo "✅ Build verified successfully!"
    startCommand: cd server && node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: DATABASE_URL
        fromDatabase:
          name: restoran-menyu-tizimi-db
          property: connectionString
    healthCheckPath: /
    autoDeploy: true

databases:
  - name: restoran-menyu-tizimi-db
    databaseName: restaurant_db
    user: restaurant_user
    plan: free
    region: oregon
    ipAllowList: []
